// pages/mock_tests.js
import React from 'react';
import AResults from '../components/admin_result'; // Adjust the path if MockTests is located elsewhere

const CreatePage = () => {
  return (
    <div>
      <AResults />
    </div>
  );
};

export default CreatePage;
